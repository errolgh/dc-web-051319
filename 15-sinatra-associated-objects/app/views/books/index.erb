<h1>Here are all the books</h1>


    <% @books.each do |book| %>
        <h2 id="<%= book.id %>">
            <a href=<%="/books/#{book.id}"%>><%= book.title %></a>
                
        </h2>
    <% end %>